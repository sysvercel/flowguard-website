import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Protect Your Property — FlowGuard Asset Protection LLC',
}

export default function WhyLayout({ children }: { children: React.ReactNode }) {
  return children
}
